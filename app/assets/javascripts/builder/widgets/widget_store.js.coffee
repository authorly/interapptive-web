# class App.Builder.Widgets.WidgetStore
  # constructor: ->
    # @widgets = []

  # addWidget: (widget) ->
    # @widgets.push(widget)

  # removeWidget: (widget) ->
    # @widgets.splice(@widgets.indexOf(widget), 1)

  # find: (id) ->
    # _.find(@widgets, (widget) -> widget.id == id)
