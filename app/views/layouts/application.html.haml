!!!

%html
  %head
    %meta{:charset => "utf-8"}
    %meta{"http-equiv" => "X-UA-Compatible", :content => "IE=edge,chrome=1"}
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1, maximum-scale=1"}
    %title= content_for?(:title) ? yield(:title) : "Interapptive"
    %meta{:content => "", :name => "description"}
    %meta{:content => "", :name => "author"}
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    :javascript
      App.currentUser(#{current_user.to_json(:only => :id)});
    = csrf_meta_tags
    = yield(:head)
  %body
    = render 'assets/upload_modal'
    = render 'storybooks/index'
    %header.navbar.navbar-fixed-top
      %nav.navbar-inner.file-menu
        #file-menu.container.left
          = render 'layouts/file_menu'
      %nav.navbar-inner.toolbar
        .container.left
          = render 'layouts/toolbar'
      %nav.navbar-inner.font-menu
        %div#font-menu-buttons.container
          = render 'layouts/font_menu'
    #main{:role => "main"}
      .container
        .content
          .row
            .span2.navbar.sidebar
              #scene-list
            .span10
              - flash.each do |name, msg|
                %div{:class => "alert alert-#{name == :notice ? "success" : "error"}"}
                  %a.close{"data-dismiss" => "alert"}
                  = msg
              %div#text
              = yield

    %footer.navbar.navbar-fixed-bottom
      %p
        %footer
          #keyframe-list
    = render 'assets/gallery_modal'
    = render 'assets/upload_download_template'
