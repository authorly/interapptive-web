require "spec_helper"

describe "User's ability to sign in and change an apps settings" do
  context "with invalid values" do
    describe "when price is negative" do
      it "should display an inline error" do
        pending
      end

      it "should change the price fields border color" do
        pending
      end
    end

    describe "when there are any empty values do" do
      it "should change the border color of any invalid inputs" do
        pending
      end
    end
  end

  context "with valid values" do
     describe "on successful response" do
       it "should close the modal" do
         pending
       end
     end
  end

end