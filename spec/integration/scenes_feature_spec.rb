require "spec_helper"

describe "User's ability to add and remove scenes from a storybook" do
  describe "adding an additional scene" do
    it "should append a scene to the top of the list" do
      pending
    end

    it "should automatically select the added scene" do
      pending
    end
  end

  describe "deleting a scene" do
    pending "absent functionality"
  end

  describe "selecting a current scene" do
    it "should change the border color when clicking a scene" do
      pending "absent functionality"
    end
  end
end