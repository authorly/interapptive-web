require 'spec_helper'

describe "User's ability to add and manipulate text on keyframes" do
  context "adding text" do
    describe "a new block of text" do
      it "should get associated with the current scene" do
        pending "absent functionality; needs merging from Widgets"
      end
    end
  end

  context "editing text" do
    describe "when double click a block of text" do
      it "should allow an editable text area" do
        pending
      end
    end
  end

  context "removing text" do
    pending "absent functionality"
  end
end