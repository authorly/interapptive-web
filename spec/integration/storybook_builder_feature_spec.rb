require 'spec_helper'

describe "User's ability to create a Storybook application" do
  context "storybooks creation and selection" do
    describe "creating a storybook" do
      pending
    end
  end

  context "adding a scene to the storybook" do
    pending
  end

  context "adding text to the current scene" do
    describe "text being added to the scene" do
      it "should not be placed in the same position as "
    end
  end

  context "uploading an image to image library" do
    pending
  end

  context "adding an image to the current scene from image library" do
    pending
  end

  context "previewing the current storybook" do
    describe "with valid prerequisites" do
      it "should not allow a preview without text or images on a keyframe" do
        pending "absent functionality; need to merge simulator branch"
      end
    end
  end
end