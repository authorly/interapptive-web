require "spec_helper"

describe "User's ability to upload and add images to scenes'" do
  it "should upload an image" do
    pending
  end

  it "should add an image to the current scene" do
    pending
  end

  it "should change the image on the current scene" do
    pending
  end

  it "should add an image on top of the background image" do
    pending "absent functionality; needs to be merged from widgets"
  end
end